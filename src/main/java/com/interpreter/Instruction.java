package com.interpreter;

public enum Instruction {
    MOV,
    ADD,
    SUB,
    MUL,
    DIV,
    AND,
    OR,
    XOR,
    NOT,
    SHL,
    LOAD,
    STORE,
    CMP,
    JMP,
    JZ,
    JN,
    JNZ,
    PRINT,
    HALT
}
